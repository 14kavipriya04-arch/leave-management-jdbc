services:
  - type: web
    name: leave-management
    runtime: docker
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: leave-db
          property: connectionString

databases:
  - name: leave-db
    databaseName: leave_management
    user: leave_user
    plan: free
